@using PagedList.Mvc
@model  EnvisionAGreenLife.ViewModel.CwList

@{
    ViewBag.Title = "Clothes Washer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Styles/Applianceslayout.css" rel="stylesheet" />
<body>
    <form asp-action="Index" method="get">
        <div class="form-actions no-color">
            <p>
                Find by brand: <input type="text" name="SearchString" value="@ViewData["currentFilter"]" />
                <input type="submit" value="Search" class="btn btn-default" /> |
                <a href="@Url.Action("Index","clothes_washer")">Back to Full List</a>
            </p>
        </div>
    </form>

    <div class="row col-lg-12 col-md-12" style="height:auto">
        @{int x = 0;}
        @if (Model != null)
        {
            foreach (var item in Model.Clothes_Washers)
            {
                x++;
                <div class="col-lg-4 col-md-4 col-sm-6 col-xs-12">
                    <div class="card" style="margin-top:50px">
                        <h2 style="text-align:center; height:50px;">@item.Brand</h2>
                        <a href='@Url.Action("Details", "clothes_washer",new { id = @item.Clothes_Washer_Id})'>
                            <img src='@Url.Content("~/Images/Clothes_Washer3.jpg")' alt="Clothes_Washer" style="width:100%" />
                        </a>
                        <p>
                            <button onclick="location.href='@Url.Action("Details", "clothes_washer",new { id = @item.Clothes_Washer_Id })'" type="button" class="Button" style="height:100px">
                                @item.Model_No
                            </button>
                        </p>
                    </div>
                </div>
            }
        }
    </div>

    <br>
    <br>
    <div class="pagecount" style="margin:auto; text-align:center">
        Page @(Model.Clothes_Washers.PageCount < Model.Clothes_Washers.PageNumber ? 0 : Model.Clothes_Washers.PageNumber)
        of @Model.Clothes_Washers.PageCount   @Html.PagedListPager(Model.Clothes_Washers, page => Url.Action("Index", new { page, currentfilter = ViewBag.currentfilter }))
    </div>
</body>
